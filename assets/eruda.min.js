// Placeholder file.
//
// The real Eruda devtools bundle (v3.4.3) is fetched here automatically
// by GitHub Actions before every build — see the "Fetch Eruda devtools
// bundle" step in .github/workflows/android.yml. This placeholder only
// exists so a local `flutter build` (without running that CI step) still
// succeeds instead of failing on a missing asset.
//
// If you're building locally and want the real inspector to work,
// replace this file's contents with the actual bundle from:
// https://cdn.jsdelivr.net/npm/eruda@3.4.3/eruda.js
//
// The app already handles this file being a stub gracefully — the
// inspector button just won't do anything until the real bundle is here.
